-- СБП-QR прямых заказов: картинка хранится приватно в БД (репо публичный).
-- Колонку обещал ещё коммит 22ddeaf (16.06) «raw-миграцией», но миграции не было —
-- боты ловили 42703 и отвечали «QR не настроен». Теперь колонка настоящая.
ALTER TABLE "GlobalSettings" ADD COLUMN IF NOT EXISTS "sbpQrBase64" TEXT;
