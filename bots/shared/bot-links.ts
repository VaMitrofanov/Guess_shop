/**
 * Опознавательные знаки в ссылках на ботов.
 *
 * Живёт в `shared`, потому что значение читают ОБЕ стороны: сайт кладёт его в
 * кнопку, бот разбирает в `/start`. Разъехавшись, они дают самый неприятный
 * вид поломки — кнопка работает, а бот делает вид, что её не было.
 */

/**
 * Гость пришёл со страницы инструкции, кода при себе нет.
 *
 * До 07.09.2026 такая кнопка вела на голый `t.me/RobloxBankBot`, и человек
 * попадал в бота, который его не знает: постоянный клиент получал апселл
 * «купи напрямую», новый — общий велком. Оба ответа мимо — он пришёл за своим
 * заказом.
 *
 * Значение НЕ должно совпадать с формой кода ВБ (семь символов A–Z0–9): бот
 * разбирает payload до похода в базу, и `WBHELP1` было бы не отличить от кода.
 */
export const TG_HELP_START = "wbhelp";

/** То же самое для ВКонтакте: `vk.me/club…?ref=WBHELP`. */
export const VK_HELP_REF = "WBHELP";
