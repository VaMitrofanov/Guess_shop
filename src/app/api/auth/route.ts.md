# Авторизация и Коридор

## Логика:
1. Юзер залогинился через ВК -> Ищем `vkId` в базе.
2. Если есть `activeCode` в куках или сессии -> Привязываем его к `User.id`.
3. Редирект юзера обратно в ВК-бота с подтверждением привязки.

## Провайдеры:
- VK ID (Standalone или Website приложение).