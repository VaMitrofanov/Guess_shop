/**
 * Заказы админки — канонический адрес.
 *
 * Гейт и логика те же самые (`requireAdmin` принимает и веб-сессию, и Bearer
 * TWA), поэтому это не вторая дверь к деньгам, а один и тот же обработчик под
 * своим именем: веб больше не обязан ходить в роут с чужим префиксом.
 *
 * `/api/twa/orders` остаётся рабочим, пока на нём живёт мобильная TWA
 * (этап В6 плана: удаляется последним, когда переезжать станет нечему).
 */
export { GET, POST } from "@/app/api/twa/orders/route";

export const dynamic = "force-dynamic";
export const revalidate = 0;
