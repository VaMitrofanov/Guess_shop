# Безопасность

Репозиторий **публичный** (`Guess_shop`). Правило: секреты, платёжные ассеты и приватные
картинки — только в env / БД, никогда в коммит.

## Что проверено

- **Секреты в коде** — не найдено. Токены (`TG_TOKEN`, `VK_TOKEN`, `.ROBLOSECURITY`,
  `DATABASE_URL`) читаются только из env.
- **TWA API** — все `/api/twa/*` роуты закрыты `extractTwaUser` (JWT `twa-admin`).
  Исключения: `auth` (точка входа) и `debug` (за `ADMIN_SECRET` + `timingSafeEqual`).
  ⚠️ Сама проверка слабее, чем кажется: `extractTwaUser` сверяет только claim `role` и не
  перепроверяет членство в `ADMIN_IDS`, а выдача токена имеет обходной Path 2 — риск №1.
- **VK-логин** — личность резолвится на сервере через VK `user_info`/`public_info`
  (`src/lib/vk-id.ts`), клиентские `vk_id`/`name` не принимаются (риск #5 закрыт).
- **initData** — HMAC-проверка подписи Telegram + отклонение старше 24 ч.
- **wb-code** — атомарный резерв в транзакции, валидация формата кода.
- **Ручной replay outbox** — только с desktop-сессией роли `ADMIN`; endpoint принимает UUID
  idempotency key и допускает исключительно `DEAD → PENDING` в serializable transaction.
  Он не умеет отправлять банк-операции, не раскрывает payload/текст ошибки и оставляет
  неизменяемый `OUTBOX_REPLAY_REQUESTED` с hash прежней ошибки. Подробнее: `payments-and-kkt.md`.

## <a name="twa-auth"></a>Известные риски

### 28. Приложение отвечает на двух хостах (`robloxbank.ru` и `www.`) — ✅ ЗАКРЫТО 2026-07-28

Оба хоста отдают приложение напрямую, без 301. При этом в production
`NEXTAUTH_URL` и `NEXT_PUBLIC_APP_URL` = `https://www.robloxbank.ru`, а `metadataBase`,
`robots.txt`, `sitemap.xml` и все внутренние ссылки = apex `https://robloxbank.ru`.

`NEXT_PUBLIC_APP_URL` подставляется в `SuccessURL`/`FailURL`/`NotificationURL` при `Init`,
поэтому клиент оформляет заказ на apex, а банк возвращает его на www. Cookie сессии NextAuth
host-only (домен не задан) → после оплаты клиент приземляется **без сессии**: шапка
показывает «Войти», ЛК пуст. Заказ виден только потому, что `/payment/status` работает по
`token` из URL.

Побочно: дубль контента для поисковиков, два независимых cookie-jar для `site_unlock`,
отсутствие `<link rel="canonical">` вообще.

**Закрыто 28.07.** Канонический хост — apex (решение владельца). `NEXTAUTH_URL` и
`NEXT_PUBLIC_APP_URL` в production переведены на apex; на Traefik добавлен роутер
`rb-www-redirect-https` (priority 200) с middleware `redirect-www-to-apex` — www отдаёт **308**
с сохранением пути и query. Перед сменой callback-адресов сделан контрольный `Init` прямо из
прод-контейнера: банк apex-адреса принимает (`Success=true`, `ErrorCode=0`). Проверено после
деплоя `c805efe`: редирект цел.

Остаточный хвост: `<link rel="canonical">` по-прежнему нет ни на одной странице — задача D11,
волна 3. Бэкап конфига Traefik: `/root/robloxbank-ssl.yaml.bak-20260728`.

### 29. Email чека нигде не подтверждается — MEDIUM, 🔴 ОТКРЫТО (2026-07-28)

Провайдер `admin-login` не проверяет `emailVerifiedAt` — войти можно с неподтверждённым
адресом. `receiptEmail` в чекауте — свободное поле: не подставляется из профиля, не сверяется
с ним и никак не подтверждается. Фискальный чек может уйти на чужой адрес, указанный
покупателем произвольно.

**Что делать:** ближайший шаг — подставлять email профиля по умолчанию и предупреждать при
расхождении. Жёсткое требование подтверждённой почты — только после стабилизации доставки
(`docs/email-setup.md`, Mail.ru appeal), иначе блокируем продажи. См. §3 S9 и задачу S9 волны 2.

### 34. В проде стоял чужой `NEXT_PUBLIC_VK_APP_ID` — вход через VK был сломан для всех — ✅ ЗАКРЫТО 2026-07-28

Владелец прислал скриншот: `id.vk.ru` встречает «Ошибка загрузки». Причина — в Coolify на
**Web и Guide** стоял `NEXT_PUBLIC_VK_APP_ID = 51912345`, тогда как настоящее приложение —
`54539012` (значение локального `.env` и фолбэка в коде). Воспроизведено headless-браузером:
`51912345` → «Error loading», `54539012` → «Sign in to «RobloxBank»».

Почему держалось долго и незаметно:

- переменная **build-time**: неверное значение запекается в клиентский бандл и живёт до
  пересборки — рестарта контейнера недостаточно;
- смоук проверял только **присутствие** бандла VK ID в чанках гейта, а не то, какой в нём
  app id. Бандл был на месте, CSP в порядке, `smoke-corridor` — зелёный;
- VK-вход отмечен в документации как «ждёт живой приёмки под настоящим аккаунтом»
  (`site-acquiring-master-plan.md`), то есть автоматически его никто и не проверял.

**Закрыто:** значение исправлено на обоих сервисах, Web и Guide пересобраны (фингерпринт
совпадает), живой прогон кнопки на `/login` открывает VK ID с формой входа в «RobloxBank».
В `smoke-corridor` добавлена проверка `VK app id в сборке гейта` (32-я): сверяет id, реально
уехавший в прод, с ожидаемым (`EXPECTED_VK_APP_ID`, по умолчанию `54539012`).

Сверка идёт по **границам числа**: простой `includes` давал ложный зелёный — произвольная
последовательность цифр находится внутри чужих чисел (например `99999999` совпадает с
float-литералами `1.0000000000001` из framer-motion). Гард проверен в обе стороны: с верным
id — зелёный, с любым другим — красный с указанием, что реально в сборке.

> Общее правило: любая `NEXT_PUBLIC_*` переменная обязана быть одинаковой на **Web и
> Guide** и требует **пересборки** обоих. Расхождение не увидит ни health, ни рестарт.

### 33. Роль админа не сверялась после входа, а источников правды было два — ✅ ЗАКРЫТО 2026-07-28

Два дефекта одного корня, найдены при подготовке переезда админки в веб:

1. `token.role` записывалась в JWT **один раз при входе** и больше не сверялась с базой
   (`src/auth.ts`, callback `jwt`). Снятие роли не действовало до перелогина: у бывшего
   админа сессия оставалась рабочей.
2. «Кто админ» решалось в двух местах — `User.role === "ADMIN"` для desktop `/admin` и
   `ADMIN_IDS` для TWA с ботами. Составы никак не связаны, при трёх админах расхождение
   гарантировано. Фактически в базе был **один** `role=ADMIN` (аккаунт с паролем без
   привязок), а в `ADMIN_IDS` — четыре Telegram ID.

**Закрыто этапом A1.** Источник правды один — `ADMIN_IDS`; роль **выводится** на каждом
запросе (`deriveSessionRole`), правило живёт в единственном `adminGrantFor`
(`src/lib/admin-grant.ts`), доступ спрашивается через единственный `requireAdmin`
(`src/lib/admin-access.ts`), принимающий и веб-сессию, и Bearer TWA. Отдельной проверки прав
внутри роутов нет — иначе появилась бы «вторая, более слабая дверь к деньгам».

Следствие, которое надо помнить: **`role = "ADMIN"` в базе сам по себе больше не даёт
админку.** Нужна проверенная TG-личность из `ADMIN_IDS` либо запасной вход владельца — адрес
в `ADMIN_BREAKGLASS_EMAILS` **и** роль в БД одновременно. При ошибке запроса к БД роль
fail-closed понижается до `USER`. Тесты: `src/__tests__/admin-grant.test.ts`, 12/12.

Осталось за владельцем: подтвердить почту break-glass-аккаунта (`emailVerifiedAt` пуст) и
сменить его пароль; задать `ADMIN_BREAKGLASS_EMAILS` в Coolify **до** деплоя.

### 32. Оплата прямых заказов в ботах идёт мимо ККТ — MEDIUM, 🔴 ОТКРЫТО (2026-07-28)

Прямые заказы в TG/VK оплачиваются переводом по статическому СБП-QR
(`bots/shared/sbp.ts`): покупатель шлёт **скриншот**, админ сверяет его глазами и вручную
подтверждает. Ни `PaymentAttempt`, ни `Receipt`, ни фискальный чек при этом не создаются —
деньги приходят вне контура эквайринга и вне 54-ФЗ.

Пока это направление было полукустарным, риск был терпимым. После подключения официального
эквайринга он становится заметным: сайт продаёт ту же услугу по тем же ценам
(`retail-direct-v1`), но с чеком, а боты — без. Замер 28.07: 34 прямых заказа всего, 3
завершены, 9 висят на шаге оплаты.

Побочно: подтверждение оплаты держится на визуальной проверке скриншота — подделать его
существенно проще, чем подписанный callback банка.

**Что делать:** перевести прямые заказы на тот же `Init`/`PaymentURL`, что и сайт —
[bot-acquiring-plan.md](bot-acquiring-plan.md). Ключевое препятствие: чеку нужен email или
телефон, а из 589 клиентов ботов контакт для чека есть у нуля. До реализации СБП-QR
остаётся, но новых нефискальных каналов открывать нельзя. Гейт B2 (письменная категория от
Т‑Банка) распространяется и на ботов.

### 30. Служебные bearer-роуты без лимитов — 🟡 ЧАСТИЧНО ЗАКРЫТО (2026-07-28)

`/api/admin/diag` и `/api/admin/wb-codes` авторизуются только `ADMIN_SECRET` (24 символа),
без rate-limit; `diag?test_tg=1` умеет отправлять сообщения в админ-чаты. Плюс `/login`
публично ссылается на `/admin/login` (`src/app/login/page.tsx:92`), бесплатно подсказывая
точку входа админа.

**Сделано 28.07:** `ADMIN_SECRET` перевыпущен (32 байта / 64 hex-символа), проверено —
старый ключ получает 401, новый 200. Значение только в Coolify production и локальном `.env`.

**Осталось:** `rateLimit` по IP на оба роута и удаление ссылки на `/admin/login` со страницы
входа клиента. См. §3 S5–S6 и волну 4.

### 31. Мёртвый `initTinkoffPayment` и диагностический JSON-лог `Init` — LOW, 🔴 ОТКРЫТО (2026-07-28)

`src/lib/tinkoff.ts:49` — легаси-инициализация платежа **без** `NotificationURL` и `Receipt`,
с рублями вместо копеек. Не вызывается ниоткуда, но пока экспортируется — любой будущий вызов
создаст нефискализированный платёж, о котором мы не узнаем.

Рядом — `logTinkoffInitExchange`: печатает точный body `Init`, включая `receiptEmail` и
**bearer-токен статуса заказа** внутри callback-URL. В production `TBANK_DIAGNOSTIC_JSON_LOGS`
корректно `false` ✅, но в preview-scope `true`.

**Что делать:** удалить обе конструкции после закрытия кейса Т‑Банка; если диагностика
остаётся — маскировать `token=` перед записью и выключить флаг в preview. См. §3 S3–S4.

### 1. TWA auth fallback (Path 2) — ✅ ЗАКРЫТО 2026-07-26
`src/app/api/twa/auth/route.ts`: если запрос без `initData`, роут доверяет `userId` из тела
и проверяет только `isAdmin(userId)`. Telegram user ID **не секретны** (видны в форвардах/чатах).
Любой, кто знает TG ID менеджера, может получить валидный 12-часовой admin-токен → полный
доступ к заказам, cookie, балансу, выкупу.

**Статус:** добавлен серверный лог `[twa-auth] hasInitData=… platform=… path2=…` (клиент
шлёт `platform` из `Telegram.WebApp`). Нужно открыть TWA на целевых устройствах (iPhone
14/15 Pro Max + MacBook 16") и проверить в логах Web-контейнера, приходит ли `initData`.
Если во всех логах `hasInitData=true` → **удалить Path 2** (строки 25–31 роута) без потери
доступа. Если какое-то устройство реально не отдаёт → заменить публичный `userId` на
подписанный `TWA_FALLBACK_SECRET`.

**Обновление 2026-07-25 (ultra-review):** риск подтверждён и уточнён — он шире, чем описано
выше. Во-первых, на `POST /api/twa/auth` **нет rate-limit**, поэтому Telegram ID можно
перебирать. Во-вторых, `extractTwaUser` (`src/lib/twa-auth.ts:75-80`) проверяет только
claim `role: "twa-admin"` и **не перепроверяет `isAdmin`** — удаление менеджера из
`ADMIN_IDS` не отзывает уже выданный токен до конца 12-часового TTL. За токеном находятся
`payments/refund`, `roblox-account/purchase`, `drain`, `purchase-batch`, `settings` и
`partners/anton/tasks`, то есть реальные деньги и робуксы. Полный план закрытия (удаление
Path 2, проверка членства на каждом запросе, `adminSetVersion` в JWT, throttle, сокращение
TTL) — `ultra-review-2026-07-25.md`, находка U1.

**✅ Закрыто 2026-07-26.** Логи Web за доступный период оказались пустыми (контейнер
пересоздавался, история `[twa-auth]` не сохранилась), поэтому «доказать, что initData
приходит всегда» было нечем — а в клиенте прямо стоял комментарий, что iOS Telegram v9.6+
`tgWebAppData` не отдаёт. Поэтому Path 2 не просто удалён, а **заменён** на путь с
проверяемой подписью:

- `POST /api/twa/auth` принимает только `initData` (HMAC Telegram) либо `linkToken` —
  токен `v1.<userId>.<exp>.<HMAC>`, который подписывает сервер бота секретом
  `TWA_LINK_SECRET` и вставляет в web_app-ссылку (`bots/shared/twa-link.ts`). Публичный
  Telegram ID не принимается ни при каких условиях; `?uid=` из клиента убран.
  Ссылка существует только внутри личного чата админа с ботом.
- `extractTwaUser` → `verifyTwaToken` теперь на **каждом** запросе сверяет `isAdmin(sub)` и
  claim `av` = хеш отсортированного `ADMIN_IDS`: правка состава менеджеров немедленно
  обесценивает все выданные пропуска.
- TTL пропуска 12 ч → **2 ч** + тихое продление в `/api/twa/ping` при активности.
- На роуте появился throttle `twa-auth:<ip>` (5 попыток, восстановление 1/мин) — после U2
  ключ ведра честный.
- `jose` (ESM-only, отсутствовал в `package.json` и тянулся транзитивно) заменён на
  HS256 на `node:crypto` — формат токена на проводе не изменился, зато путь входа стало
  возможно покрыть тестами: `src/__tests__/twa-auth.test.ts` (11 тестов, в т.ч. «валидно
  подписанный токен с чужим `sub` отклоняется»).

Прод-приёмка 26.07: `POST /api/twa/auth` с телом `{"userId":<реальный admin id>}` → **400**,
токена нет; с `{"linkToken":"<подделка>"}` → **401**.

### 2. Rate-limiting на публичных API — ✅ ОБХОД ЗАКРЫТ 2026-07-26
`/api/wb-code` за in-memory token-bucket (`src/lib/rate-limit.ts`): POST burst 10 / 1-в-5с,
GET burst 20 / 1-в-2с, 429 + `Retry-After`. Код верный — **прямой тест контейнера**
(в обход прокси, ключ «unknown») даёт ровно 20 проходов + 10×429.

**Ограничение прода:** цепочка **Cloudflare-tunnel → Traefik НЕ пробрасывает реальный IP
клиента** в контейнер — приложение видит `X-Forwarded-For`/`X-Real-Ip` = IP хоста
(значение хранится только в `HANDOFF.md`) для всех запросов, `cf-connecting-ip` не доходит. Значит per-IP лимит
вырождается в грубый глобальный троттл (все клиенты в одной корзине) и не различает атакующего
от легитимных. Диагностический лог `[wb-code] POST reserve … ip=… cf=… xff=…` добавлен для
подтверждения в проде.

**Что делать (на выбор):** (а) настроить проброс client-IP в Traefik (trusted forwarded
headers) + cloudflared, тогда per-IP лимит заработает как задумано; (б) перенести
rate-limiting на **Cloudflare WAF** (rate-limiting rules) — естественное место, раз трафик и
так через CF; (в) оставить как есть (грубый троттл + abuse-лог). Лимитер безвреден и остаётся.
Также: при масштабировании на >1 реплику Web нужен общий стор (Redis) вместо in-memory Map.

**Обновление 2026-07-16:** отдельные buckets теперь есть на регистрации, quote, checkout,
Roblox games/gamepasses, TG login challenge, identity-link, WB reserve/read,
`select-gamepass`, polling статуса SITE-заказа и client observability. Для текущей одной
Web-реплики это закрывает основной site abuse-scope; перед горизонтальным масштабированием
по-прежнему нужен общий Redis/WAF limiter и production-подтверждение реального client IP.

**Обновление 2026-07-04:** DNS переключён на **прямой A к RF-хосту**
(grey-cloud, минуя Cloudflare-tunnel — см. риск #7). Теперь client-IP в принципе доходит до
Traefik напрямую; вариант (а) стал реалистичным — проверить лог `[wb-code] ip=` после
переключения и, если IP реальный, включить per-IP лимит. Одновременно ушла CF-защита перед
источником → см. риск #7 (публичный IP).

**Обновление 2026-07-25 (ultra-review) — статус ухудшается до 🔴 P0:** переключение на
grey-cloud имело незамеченное последствие. `clientIp()` (`src/lib/rate-limit.ts:79-90`)
безусловно доверяет `cf-connecting-ip`, затем `true-client-ip`, затем `x-forwarded-for` —
это логика времён Cloudflare-туннеля. Проверка 25.07 подтверждает, что туннеля больше нет:
`dig robloxbank.ru A` даёт RF-адрес вне диапазонов Cloudflare, а ответ прода не содержит ни
`cf-ray`, ни `server: cloudflare`. Значит `cf-connecting-ip` сейчас — **обычный клиентский
заголовок**, и достаточно менять его в каждом запросе, чтобы полностью обойти все
IP-лимиты: `wb-code` (перебор и разведка кодов), `pricing/quote`, `orders/create`, IP-ведро
подбора пароля (`auth-throttle.ts`), `observability/client`, опрос статуса заказа. Устоит
только email-ведро, оно не привязано к IP. Прямое следствие — неаутентифицированный
`POST /api/pricing/quote` неограниченно создаёт строки `PriceQuote` в продовой БД, ретенции
для них нет. План (флаг `TRUSTED_PROXY_MODE`, разбор XFF по ближайшему hop, unit-тест на
подделку заголовка, опционально возврат orange-cloud + WAF) — `ultra-review-2026-07-25.md`,
находка U2.

**✅ Закрыто 2026-07-26.** `clientIp()` переписан: режим доверия задаётся env
`TRUSTED_PROXY_MODE` (`direct` по умолчанию, в проде выставлен явно).

- `direct` — берётся **последний** hop `x-forwarded-for`, то есть адрес, который дописал
  наш Traefik. Всё, что клиент прислал левее, игнорируется; `cf-connecting-ip` и
  `true-client-ip` не учитываются вовсе.
- `cloudflare` — `cf-connecting-ip` принимается, только если ближайший hop входит в
  публичные диапазоны Cloudflare (список в коде, переопределяется `CLOUDFLARE_IP_RANGES`).
  Режим готов к возврату orange-cloud, но сейчас не включён.

Тесты `src/__tests__/rate-limit.test.ts`: 30 запросов с разными подделанными
`cf-connecting-ip` дают **один** ключ ведра. Прод-приёмка 26.07 — 30 запросов к
`/api/wb-code` с ротацией заголовка упёрлись в 429 по реальному IP.

Побочно закрыт вектор роста БД: анонимный `POST /api/pricing/quote` больше **не создаёт**
строку `PriceQuote` (потребить её всё равно нельзя — `validateCheckoutQuote` требует
совпадения владельца), плюс появилась ретенция (см. риск №27).

### 7. Прямой публичный IP источника после ухода с Cloudflare-tunnel — 🆕 (2026-07-04)
Раньше `robloxbank.ru` резолвился на Cloudflare (proxied) и трафик шёл через
`cloudflared`-туннель — источник был скрыт за CF (DDoS-защита, WAF). На 04.07 DNS —
**прямой A на RF-сервер** (точный адрес только в `HANDOFF.md`), сертификат Let's Encrypt. Причина смены —
деградация Cloudflare у российских розничных провайдеров (ТСПУ): сайт «работал только с VPN»
(жалобы клиентов, «вечное восстановление сессии»). Побочный эффект — **IP источника публичен**:
прямой вектор для DDoS/сканов, CF-WAF больше не прикрывает. ⚠️ **Кто переключил DNS —
подтвердить у владельца** (в HANDOFF записей нет; если не он — инцидент доступа к CF-аккаунту).
Митигации (на решение владельца): Traefik rate-limit middleware / CrowdSec (уже есть контейнер
`crowdsec` на хосте) / вернуть CF proxy только для не-РФ гео. `panel.robloxbank.ru` остаётся
proxied (CF) — это ок.

### 8. Google Sheets credentials для B2B sync — 🆕 (2026-07-09, sync включён 2026-07-10)
B2B-режим `Антон` использует Google Sheets sync/write-back (`src/lib/google-sheets.ts`).
Для подключения нужен service-account JSON с доступом к таблице. Это секрет: хранить только в env/Coolify или
локальном `HANDOFF.md`, не в `docs/` и не в коммитах. Публичная документация может описывать
только имена env, схему колонок и правила sync. Саму таблицу лучше шарить на конкретный
service-account с минимальным доступом Editor только к нужному документу, а не на весь диск.
Для Антона публично фиксируется только `spreadsheetId`, маппинг колонок и статусы; локальный
путь к JSON и содержимое credentials остаются вне git. Прод-подключение использует env
`ANTON_GOOGLE_SHEETS_SPREADSHEET_ID` и `GOOGLE_SHEETS_SERVICE_ACCOUNT_JSON`; значение JSON
нельзя логировать и нельзя добавлять в `.env.example`/docs. `google-sheets.ts` не логирует
JSON credentials, private key, access token и raw-ответы Google, а `GoogleSheetsError` несёт
только короткий текст. Google Sheet расшарен на service-account email из JSON с правом Editor
(write-back пишет в `D/E`); scope ограничен `spreadsheets`. Sync ходит в Google только с
сервера — клиентский TWA работает с задачами из БД, а не с таблицей напрямую.
С Этапа 5.8 (2026-07-11) бот дополнительно лочит выполненные строки `addProtectedRange`
(анти-tampering: сотрудники не могут править/удалять уже оплаченные строки `A:D`).
Редакторы защиты берутся из `client_email` credentials + env
`GOOGLE_SHEETS_PROTECTED_EDITORS` (email владельца таблицы) — email'ы не хардкодятся
в публичном репозитории.

TWA также принимает ручной `.xlsx` upload для Антона как запасной путь импорта. Ограничения:
до `5 MB`, до `300` строк, бинарный файл не сохраняется, в БД остаются только metadata строки.
В такие файлы нельзя добавлять cookie, seed-фразы, токены, service-account JSON и другие
секреты. Зависимость `xlsx` нужно держать обновлённой и проверять через `npm audit`, потому
что парсинг офисных файлов расширяет поверхность атаки.

### 9. Локализация и трансграничная передача ПД — P0, 🔴 ОТКРЫТО (2026-07-12)

**Повторная production-сверка 17.07:** публичная `/legal/policy` утверждает, что оператор
обеспечивает локализацию ПД граждан РФ, одновременно называя `Vercel Inc. / провайдер VPS`,
но primary Postgres по-прежнему находится вне РФ. Это не косметическая неточность, а
фактическое противоречие между опубликованной политикой и инфраструктурой. До формальной
заявки эквайринга нужны российский primary, data map, трансграничное решение,
restore/reconciliation и юридическая редактура документа. На mobile `390 px` дополнительно
обнаружено обрезание заголовка политики (`scrollWidth 408 > clientWidth 386`).

**UI-fix 17.07, production `b6b699f`:** общий legal shell получил
`overflow-wrap`/`overflow-x-clip`; проверка viewport `390×844` показывает
`scrollWidth 386 == clientWidth 386`, заголовок целиком внутри viewport.
Инфраструктурное противоречие локализации остаётся P0.

Основная production-БД размещена в зарубежном регионе провайдера, при этом публичная
политика конфиденциальности заявляет хранение/локализацию в РФ. В `User` и заказах есть
идентификаторы TG/VK, email, имена, Roblox username, IP принятия оферты и история покупок —
это не только технические данные. Для граждан РФ статья 18(5) 152-ФЗ требует обеспечить
первичную запись/систематизацию/накопление/хранение/уточнение/извлечение через базы в РФ;
отдельно нужно проверить уведомление Роскомнадзора и трансграничную передачу.

**Риск:** несоответствие фактической инфраструктуры опубликованной политике и требованиям
локализации; регуляторный риск усиливается перед публичным checkout и эквайрингом.

**До запуска:** составить data map и основания обработки; выбрать российский primary
Postgres; сделать backup/restore drill, миграцию и сверку количества пользователей,
заказов и бонусов; только затем исправить политику. Зарубежные subprocessors и реплики
оформить как отдельный трансграничный контур после юрпроверки. Секретные host/credentials
остаются только в env/`HANDOFF.md`.

### 10. Checkout/webhook Т-Банка — P0, 🟡 КОД/WORKER/REFUND ГОТОВЫ, ВНЕШНИЕ GATES ОТКРЫТЫ (2026-07-13)

**Было:** legacy callback подтверждал `OK` даже неверную подпись/исключение, не сверял
`TerminalKey/PaymentId/OrderId/Amount`, а checkout доверял клиентской сумме и не проверял
owner/цену геймпасса.

**Код 2026-07-13; UX guard выровнен 2026-07-16:** checkout принимает только owned active
quote, проверяет TTL/version, Roblox owner/sale-state и gross price с тем же узким допуском
±2 R$, что production buyout guard (компенсирует только целочисленное округление комиссии),
а также обязательный UUID. Произвольная цена и чужой/снятый с продажи пасс блокируются.
Quote, SITE-order,
`PaymentAttempt`, event и outbox создаются атомарно; публичный статус защищён случайным
token (в БД только SHA-256) либо сессией владельца. Callback возвращает `OK` только после
strict signature/terminal/payment/order/amount checks и serializable commit, разрешает
только монотонные переходы и дедуплицирует события. Временная ошибка получает `503`, а не
ложный `OK`. Raw provider payload/PII в event не сохраняется.

**Риск:** потерянная подтверждённая оплата, двойное выполнение, подмена параметров заказа,
вечные PENDING/PROCESSING и невозможность надёжной сверки/возврата.

**Добавлено:** TG long-running worker с lease/retry/dead-letter, идемпотентный full/partial
refund через `/v2/Cancel`, partial-refund receipt, cumulative refund accounting и запрет
blind retry при неизвестном результате provider call.

**Остаток до запуска:** kill-switch остаётся false. Нужны реальные DEMO-terminal callbacks,
fault injection, официальный прогон «Общие»/«Формирование чека», сверка ККТ/ОФД и ручной
replay/reconciliation UI. До этого риск остаётся P0 и maintenance снимать нельзя. Полная
матрица — `payments-and-kkt.md`.

### 11. Связывание TG/VK/email-аккаунтов — HIGH, 🟡 TG FLOW ЗАКРЫТ, VK LINK/UNLINK ОСТАЛИСЬ (2026-07-15)

БД у каналов общая, legacy TG/VK/email-поля остаются в `User`, а серверно подтверждённые
субъекты также хранятся в `UserIdentity`. Автослияние по Roblox username небезопасно: в
production уже есть одинаковый ник у разных профилей. Обычный VK-login поэтому не
показывается как безопасная привязка/merge.

**Риск:** захват/переключение аккаунта, потеря видимости заказов, ошибочное объединение и
двойное либо потерянное списание бонусов.

**Сделано:** внешний iframe Telegram Login Widget удалён. Web создаёт криптографически
случайный challenge с TTL 5 минут, хранит только SHA-256 state и передаёт raw state боту в
deep link. Бот строит callback из Telegram-профиля и HMAC-подписывает его своим токеном;
Web повторно проверяет подпись/возраст и атомарно потребляет challenge ровно один раз.
Login резолвит legacy TG-профиль через `UserIdentity`. Link challenge выдаётся только при
web-session моложе 10 минут, заранее привязан к target User, запрещён для ADMIN и после
подтверждения переносит identities/orders/intents и бонус через ledger с
`AccountMergeAudit`. Имя, email и Roblox nick доказательством не являются.

**До запуска:** live Telegram login/link после deploy под реальным пользователем, негативная
merge matrix на prod-like clone, аналогичные явные link/unlink для VK и
recovery/admin-console. Source-профиль пока остаётся инертным audit anchor;
автоматического rollback UI нет.

**Обновление 28.07.2026.** Живой TG login/link под реальным пользователем **пройден**:
владелец завёл аккаунт по почте, привязал Telegram, профили слились
(`AccountMergeAudit` = `COMPLETED`), заказы и бонусы переехали.

Отсутствие VK-привязки тоже подтверждено вживую и уже даёт наблюдаемый эффект: вход через
VK создал **отдельный профиль** рядом с существующим TG+email, а не привязался к нему.
Практические следствия, которые надо помнить до реализации VK-link:

- клиент, у которого заказы под TG, после VK-входа увидит **пустой кабинет** и решит, что
  история потерялась;
- **вход через VK не даёт админку** — админом делает TG-личность из `ADMIN_IDS`, а она
  остаётся на другом профиле. Это работает в нашу пользу: случайный VK-вход владельца не
  создаёт второй привилегированный аккаунт.

### 14. Выкуп геймпасса по live-цене без сверки с номиналом (TOCTOU) — ✅ ЗАКРЫТО (2026-07-12), пробел в TG закрыт 2026-07-17

**Инцидент 12.07:** клиент подложил один геймпасс в два заказа и поднял его цену между
приёмом ботом и ручным выкупом (715 → 1143 R$). TWA-выкуп покупал по live-цене без сверки
с номиналом заказа — заказ на 500 закрылся покупкой за 1143 (перерасход 428 R$ с донора).
Цена проверялась только в момент приёма пасса ботом, не в момент покупки.

**Фикс (PLAN-gp-price-guard Ш1–Ш4):** единый гард `src/lib/purchase-guard.ts`
(`expected = ceil(amount / 0.7)`, допуск ±2 R$ — эталон автовыкупа) на всех точках выкупа:

- `api/twa/orders` `purchase` — блок до покупки с пометкой `[ЦЕНА-СТОП]` в adminNote
  (статус заказа не меняется); туда же перенесён seller-check автовыкупа (`[ПРОДАВЕЦ-СТОП]`:
  создатель пасса ≠ подтверждённому нику заказа). Гонку «цену подняли между проверкой и
  POST» отклоняет сам Roblox — `expectedPrice` в теле покупки равен только что
  провалидированной цене.
- `api/twa/orders` `purchase-script` — при расхождении базы скрипт не выдаётся (409); в сам
  скрипт зашита server-resolved buyer-price поверх проверенной базы, устаревший скрипт
  Roblox отклонит. Это также безопасно поддерживает typed Roblox Plus.
- ⚠️ **Пробел, найденный 17.07:** гард `Ш2` доехал только до TWA. TG-кнопка «📋 Скрипт»
  (`ps:` в `bots/tg/handlers.ts`) сверки с номиналом **не имела** и зашивала live-цену —
  тот же сценарий инцидента был воспроизводим через TG. Закрыто 17.07 вместе с переходом
  на браузерный скрипт: сверка `|base − ceil(amount/0.7)| ≤ 2` и зашитая ожидаемая цена.
  Урок: точек выкупа четыре (TWA orders, TWA account, TG script, автовыкуп) — гард нужно
  проверять на каждой, «закрыто» по одной ≠ закрыто.
- `api/twa/roblox-account/purchase` — если пасс привязан к активному заказу, цена покупки
  сверяется с номиналом этого заказа (409 при расхождении); свободная покупка без заказа
  не блокируется, но поиск показывает ожидаемую цену привязанного заказа.
- UI: карточки выкупных статусов в TWA показывают «⚠️ цена ≠ номиналу» до нажатия
  «Выкупить» (данные `gp-live-check`).

Легальный путь при расхождении — привести заказ/пасс в порядок (замена пасса клиентом,
`edit-order` из карточки, `attach-gamepass`), «мягкого» override нет. Обратный порядок
атаки закрыт 17.07: pre-existing ownership/`AlreadyOwned` больше не является автоуспехом.
Retail-заказ получает `ERROR/GAMEPASS_REUSED` и `[ВЛАДЕНИЕ-СТОП]`, после чего требуется
ручная сверка возможного переиспользования ГП.

**Ремедиация инцидента (12.07):** данные пострадавшего заказа скорректированы под новый
пасс клиента с корректной ценой (номинал приведён к фактическому остатку долга, аудит в
adminNote); остаток выплачивается обычным выкупом. Для таких правок в карточке заказа
появился `edit-order` («✏️ Редактировать», см. docs/twa-admin.md) — раньше требовался
прямой SQL.

**Регрессия Managed/Regional Pricing (14.07) — закрыта.** Roblox включает региональные
цены пассов по умолчанию: публичный product-info показывал глобальную цену продавца, а
cookie-аккаунт имел другую buyer-specific цену. Передача публичной цены в `expectedPrice`
давала чистый отказ `PriceChanged`; 12 валидных заказов оказались в ERROR. Денежные пути
теперь по умолчанию fail-closed получают product-info с buyer-cookie только с официального
`apis.roblox.com` и валидируют заказ по `UserBasePriceInRobux`. Если `PriceInRobux`
отличается, покупка запрещена: основной TWA-путь ищет у того же ника свободную full-price
замену, проверяет её цену, продавца и отсутствие во владении; без замены ставит
`ERROR/REGIONAL_PRICE`. Клиентский `price/productId/sellerId` не является источником истины;
cookie не передаётся proxy-fallback. Регресс покрыт unit-тестами регионального стопа.

**Контролируемое исключение 14.07 — удалено.** Scoped `PAYOUT_EXPERIMENT` доказал, что
legacy v1 возвращает `PriceChanged`, но не стал постоянным обходом. Рабочий контракт не
принимает скидку из клиента: сервер читает свежий authenticated product-info и распознаёт
только один арифметически корректный detail `RobloxPlusSubscription` на 10% или 20%.
Unknown, mixed или поддельный detail остаётся fail-closed.

Production-попытка с корректной buyer-price вернула `PriceChanged`, balance/ownership не
изменились. Повторный аудит `PriceDiscountDetails` уточнил угрозу: все 12 случаев имели
typed-причину `RobloxPlusSubscription:10`, а не Regional Pricing. Roblox Plus официально
субсидирует 10–20% и сохраняет доход продавца. Реализованный контракт allowlist-ит только
typed Plus-detail, проверяет процент, сумму скидки и base-price, а любой unknown/mixed
discount оставляет fail-closed. Реальный canary показал, что legacy v1 отклоняет и base,
и buyer-price как `PriceChanged`, а `/v2/user-products/{productId}/purchase` для pass
возвращает 404. Баланс, ownership и заказ не изменились. 17.07 официальный client flow
поднят в настоящем Chrome: cookie-инъекция в чистый профиль доказана покупкой, а typed Plus
передаёт проверенную buyer-price только после сверки base/арифметики скидки. Unknown/mixed
discount остаётся fail-closed; guessed endpoints не используются. Покупка считается успехом
только при ownership `false→true` и точной дельте баланса. Полный план —
`docs/roblox-plus-buyout-plan.md`.

### 15. Компрометация кода до покупателя (ПВЗ-фрод) — HIGH, 🔴 ОТКРЫТО, план утверждён (2026-07-13)

Код на вкладыше — единственный секрет, а активация работает по принципу «кто первый ввёл».
Вкладыш можно вскрыть/сфотографировать до того, как карта попадёт к честному покупателю:
типовой сценарий — заказ карты, вскрытие упаковки на пункте выдачи, отказ от товара; карта
возвращается в оборот и продаётся повторно с уже использованным кодом. Подтверждённый
инцидент 2026-07-13 (детали и форензика — локально, вне публичного репо).

**Риск:** честный покупатель получает мёртвый код; выплата уже ушла злоумышленнику;
серийный вариант (сотрудник ПВЗ) масштабируется.

**Утверждённый план (`PLAN-wb-antifraud`, локальный):**

- **A — физическая защита тиража:** скретч-слой/запечатанный конверт + предупреждение
  «повреждён — откажись от товара» на упаковке и в карточке товара.
- **B — гейт активации по данным продавца WB:** синк `orders`/`sales` statistics-api в
  локальную таблицу; активация кода требует непотреблённой подтверждённой продажи того же
  номинала (1 продажа = 1 код). Отказ на ПВЗ продажи не создаёт → активация по фото вкладыша
  блокируется. Точный ключ сверки — номер этикетки с пакета (есть в API и у покупателя в
  руках). Режимы off/monitor/enforce, fail-open при устаревшем синке, холд выкупа до
  подтверждения продажи.
- **E — процесс инцидентов:** IP/UA-форензика активации, диспут-флоу «код уже использован»
  в ботах с автосбором доказательств и карточкой менеджеру, таблица блэклиста с мягким
  холдом выкупа.

До внедрения B основная митигация — ручной разбор жалоб менеджером.

### 16. CSP/VK ID provider — 🟡 КОД И ПУБЛИЧНЫЙ CONTROL ВОССТАНОВЛЕНЫ (2026-07-19), full-account acceptance открыт

**Повторная production-сверка 17.07:** CSP contract и нужные `vk.ru` hosts были зелёными,
но старый скрытый OneTap iframe завершался `timeout`, из-за чего наша видимая кнопка
навсегда оставалась disabled. Ошибка была client-side и не оставляла серверного следа.

**Корневой фикс 19.07:** `VKAuthButton` использует официальный bundled npm SDK
`@vkid/sdk@2.6.6` и открывает `VKID.Auth.login` в режиме `InNewWindow` прямо из
пользовательского клика. Скрытый OneTap DOM bridge, задержка готовности и синтетический
`target.click()` удалены. Кнопка активна сразу; popup не зависит от загрузки iframe.
Клиент по-прежнему передаёт серверу только raw access token, а `vk_id`/имя/avatar сервер
получает сам через VK. Регрессию фиксирует `src/__tests__/vk-auth-client-flow.test.ts`.

`NEXT_PUBLIC_VK_AUTH_ENABLED` остаётся fail-closed: без точного `true` VK ID скрыт из
`/login`, а Guide показывает прямую ссылку на сообщество. После production deploy флаг
включается только в Web/Guide build-time+runtime env. Полный callback acceptance на
реальном VK-аккаунте остаётся отдельным ручным тестом; CAPTCHA нельзя автоматизировать.

Введённый в рамках риска №13 Content-Security-Policy (`next-security.ts`) разрешает
`connect-src`/`frame-src` только для `id.vk.com` / `oauth.vk.com` / `api.vk.com`.
Установленный VK ID SDK собирает все свои
эндпоинты от домена **`vk.ru`**: `id.vk.ru`, `oauth.vk.ru`, `api.vk.ru`, `login.vk.ru`.
Обмен кода (`VKID.Auth.exchangeCode` → `fetch https://id.vk.ru/oauth2/auth`) блокируется
браузером ещё до нашего сервера: клиент проходит окно VK и получает «Ошибка авторизации».

**Затронуто с деплоя 13.07:** кнопка VK на WB-гейте, VK-кнопка в инструкции (шаг 06),
вход VK на `/login` и связывание VK в кабинете. Telegram-пути не затронуты. Серверных
следов у ошибки нет (она client-side) — поэтому поломка жила незамеченной.

**Фикс (реализован 2026-07-14):** vk.ru-хосты добавлены в `connect-src` и `frame-src`
(`next-security.ts`); задеплоены Web **и вручную Guide** (гейт отдаёт CSP из своей
сборки). Регрессия закрыта двумя механизмами:

- контракт-тест `src/__tests__/csp-vk-hosts.test.ts` — извлекает базовые VK-домены из
  установленного npm-бандла и требует их id/oauth/api/login-хосты в CSP; любой апдейт SDK или
  правка CSP без синхронизации валит CI;
- синтетический смоук `scripts/smoke-corridor.mjs` — гейт, чанки `/_next-guide`,
  vendor SDK, CSP-хосты, `/api/wb-code` (+ `--reserve` живой резерв, `--alert` в TG);
  ненулевой exit code при любом фейле, пригоден для cron.

Остаток: полный живой прогон callback/login/link/unlink на реальном VK-аккаунте; полный
мониторинг коридора по расписанию — карточка Trello от 2026-07-14.

### 17. Клиентская телеметрия сайта — 🟢 PII-SAFE FOUNDATION (2026-07-16)

`SiteObservability` отправляет только тип сигнала, pathname без query, известное имя Web
Vital, численное значение/rating либо локальный 8-символьный fingerprint ошибки. Raw error
message, stack, query string, email, order token, cookie и user/session ID контрактом
запрещены (`ClientSignalSchema`). Серверный endpoint ограничен token-bucket; в TG уходят
только poor/error-сигналы и не больше четырёх алертов за burst с медленным refill.

Разбор owner-скриншота 16.07 подтвердил: `FCP/LCP/TTFB/CLS` — это измерения скорости и
стабильности интерфейса, не ошибки Telegram-бота. Повторяющийся `TypeError` шёл из `/guide`;
для старых WebView закрыты небезопасные обращения к `matchMedia(...).matches` и
`IntersectionObserver`. Fingerprint теперь строится без build-chunk filename, поэтому один
дефект сохраняет один идентификатор между deploy. Raw message по-прежнему не собирается.

Это базовая наблюдаемость, не полноценный APM: server logs нужно подключить к retention/
дашборду, а перед несколькими Web-репликами перенести alert bucket в общий store. Нельзя
расширять payload raw exception/URL без отдельного privacy-review.

Сигналы по pathname `/twa` относятся к TWA и проверяются отдельно от внешних шаблонных
проектов. Уведомление с pathname вроде `artifact-template-analytics-dashboard` не является
сигналом RobloxBank и не диагностирует этот репозиторий.

### 18. Новый purchase API не сверяет продавца — [ПРОДАВЕЦ-СТОП] остался без второй линии — 🟡 ПРИНЯТО (2026-07-17)

Старый `economy/v1/purchases/products` принимал в теле `expectedCurrency` + `expectedPrice`
+ `expectedSellerId`, и Roblox **сам** отклонял покупку при несовпадении продавца. Это была
вторая линия защиты после нашего seller-check.

Новый `game-passes/v1/game-passes/{productId}/purchase` принимает в теле **только**
`expectedPrice` (подтверждено разбором клиентского кода Roblox: ветка `"Game Pass" ===
assetType` шлёт `{expectedPrice}`). Продавец не сверяется сервером Roblox вообще.

Последствие: `[ПРОДАВЕЦ-СТОП]` теперь **единственная** защита от подмены продавца — сценарий
общего/чужого ГП (инцидент 12.07, `YTGQ5BQ`/`QARJR71`) ловится только нами. Цена по-прежнему
имеет вторую линию: `expectedPrice` уходит в Roblox, расхождение = `PriceChanged`.

Митигация: seller-check продублирован на всех точках — сервер (`sellerMatchesOrder`,
`purchase-guard.ts`) и сам браузерный скрипт (`roblox-purchase-script.ts` сверяет
`info.Creator.Id` с зашитым числом до открытия окна покупки). Дублирование в скрипте
намеренное: тот же текст исполняется и purchase-service, и браузером менеджера.

Purchase-service слушает только SG docker bridge, защищён отдельным Bearer-токеном и
single-flight очередью; Web/TWA подключён через ограниченный SSH-туннель. Cookie не
логируется и не пишется в отдельное хранилище сервиса; она остаётся в изолированном Chrome
profile. `AlreadyOwned`/pre-existing ownership — стоп, а не
успешное завершение заказа.

### 19. Donor-cookie используется из двух egress/context и инвалидируется до покупки — P0, 🟡 DEPLOY/READINESS ГОТОВЫ; CANARY ОТКРЫТ (2026-07-17)

Новый browser transport перенёс только финальный purchase в persistent Chrome на SG, но
authenticated preflight остался в Web/TWA на RF: cookie-save/refresh, balance,
buyer-specific product-info, authenticated user и часть ownership/drain checks выполняются
прямым Node fetch. Затем та же cookie инъецируется в Chrome с другого egress/context.

Production-доказательства: service/Chrome/tunnel healthy, очередь 0; обе реальные попытки
`NotLoggedIn`; cookie присутствует в Chrome с правильными атрибутами, но authenticated user
и currency дают 401. Свежая cookie была сохранена за две минуты до второй попытки. Заказ не
закрыт, списания и нового ownership нет, автомат OFF. Canary, выполненный целиком на SG, эту
границу не проверял.

Риск: Roblox может инвалидировать donor session из-за смены IP/региона/device/HBA context.
Повторная загрузка cookie через текущий TWA даёт ложноположительное «валидна», а generic
`BrowserUnavailable` скрывает необходимость заменить cookie. Автоматические ретраи способны
сжечь новые funded sessions и остановить всю очередь.

Митигация задеплоена: один no-backlog single-flight browser-auth boundary на SG
для session, currency, buyer product-info, ownership и purchase; authenticated direct-fetch
и fallback с RF/Node удалены из donor-путей. Добавлены стабильные error codes,
PII/secret-safe structured logs и contract-test, запрещающий `.ROBLOSECURITY` в Roblox
headers вне browser service. Отдельный cookie аккаунта-приёмника drain остаётся в своём
контуре и не передаётся donor helpers. SG service, Web→SG и TG→SG прошли readiness smoke с
тестовой невалидной строкой; до новой cookie и canary 3/3 все денежные автоматические пути
выключены. План и acceptance — `docs/roblox-plus-buyout-plan.md`,
deploy-инвариант — `docs/deploy.md`.

Дополнительный donor guard: buyer-specific preflight возвращает account ID вместе с
gamepass-данными. Ручной скрипт не может быть выдан с пустым `buyerUserId`, а фоновые
воркеры при недоступной/невалидной session переходят в 15-минутный backoff вместо частых
повторов с той же cookie.

### 20. Email account lifecycle и доказательство consent — 🟡 КОД ГОТОВ / SMTP ACCEPTANCE (2026-07-18)

Регистрация требует `agreedToPrivacy: true` и bcrypt-хеширует пароль, однако email не
проверяется одноразовой ссылкой, reset-пути нет, а согласие не сохраняется как versioned
evidence. Поэтому `EMAIL` identity нельзя трактовать как доказанную пользователем до
завершения lifecycle.

**Закрыто 17.07 — throttling password sign-in.** `CredentialsProvider` больше не проверяет
пароль без лимита: `src/lib/auth-throttle.ts` ограничивает попытки по IP (строго) и по
нормализованному email (сеть безопасности) **до** обращения к БД и bcrypt. Это закрывает и
второй, менее очевидный вектор: bcrypt cost 12 = ~250 мс CPU на попытку при 2 vCPU, то есть
безлимитный вход был ещё и дешёвым CPU-DoS. Ёмкость email-ведра намеренно больше IP-ведра,
иначе один хост выжигает бюджет жертвы и **блокирует ей вход** (поймано тестом на первой
версии). Инвариант закреплён тестом.

**Закрыто в коде 18.07:** verification/resend и password reset используют random 256-bit
bearer, в БД хранится только SHA-256; TTL 24 часа/30 минут, consume-once выполняется
атомарно. Регистрация даёт generic anti-enumeration ответ, `EMAIL` identity создаётся лишь
после verification. `ConsentEvidence` хранит version/timestamp/source/keyed-IP-hash без
сырого IP/UA. Reset пишет bcrypt-12 и увеличивает `sessionVersion`; JWT другой версии
инвалидируется. Legacy JWT без версии намеренно потребует повторный вход один раз после
rollout, иначе такой токен нельзя было бы отозвать.

**Закрыто 27.07 — внутренний origin в verification redirect.** За reverse proxy
`req.nextUrl.origin` у Route Handler равнялся внутреннему `https://0.0.0.0:3000`. Токен
успешно потреблялся, но финальный `303 Location` уводил пользователя на локальный адрес;
кроме UX-дефекта это делало результат зависимым от proxy/Host metadata. Verification route
теперь строит абсолютный redirect только из allowlist-origin (`robloxbank.ru`, `www` или
`localhost` в dev), а внутренний/чужой host откатывается на `https://robloxbank.ru`.
Регрессия закрыта unit-тестом и проверкой production-заголовка `Location`.

**Статус 28.07:** Brevo SMTP, DKIM/DMARC, branded SPF/return-path, Gmail-доставка и
verification redirect приняты в production. Mail.ru отклоняет общий Brevo IP с
`550 spam message rejected`; appeal отправлен через официальную форму провайдера.
Интерфейс не обещает доставку после одного SMTP acceptance и предлагает Telegram/другой
email. Входящий MX/служебный ящик остаётся отдельной задачей. Детали: `docs/email-setup.md`.

**Риск (остаточный):** до SMTP acceptance самостоятельный recovery недоступен; in-memory
лимит не заменяет shared limiter при нескольких web-репликах; абсолютный/idle TTL и кнопка
«выйти на всех устройствах» остаются отдельным P1.

### 22. Название «Roblox Bank» — юридический / комплаенс риск — 🟡 ЧАСТИЧНО ЗАКРЫТО (2026-07-18)

**Риск A — ТЗ «Roblox»:** используется в коммерческом имени, домене и брендинге без лицензии
Roblox Corporation. Возможен cease-and-desist, UDRP-спор о домене, блокировка API.

**Риск B — слово «Банк»:** ФЗ-395-1 ст. 7 ограничивает использование слова «банк» в
наименовании лицензированными кредитными организациями. ИП не является кредитной
организацией → формально нарушение. ЦБ РФ / ФАС могут потребовать убрать.

**Риск C — Т-Банк комплаенс:** название «Roblox Bank» у мерчанта может быть расценено как
мимикрия под финансовую организацию. Возможен отказ в подключении боевого терминала.

**Закрыто 18.07 дисклеймерами:** footer, оферта §2 и §11, политика §1 и §5, реквизиты §3 —
явное отрицание статуса банка (ссылка на ФЗ-395-1), nominative fair use для ТЗ Roblox,
указание что карточные данные обрабатывает только Т-Банк. Дисклеймеры **смягчают** риск,
но **не устраняют** его — полное закрытие = переименование (варианты в master-plan §5.1).

### 23. Checkout return и WB channel handoff — 🟡 RETURN ЗАКРЫТ / WB ПЛАН (2026-07-18)

**Checkout return:** обязательный login перед оплатой добавляет redirect-параметр `next`.
Без проверки это open redirect после успешной авторизации и удобная фишинговая цепочка.
В коде разрешён только same-origin path до 2048 символов; external URL, protocol-relative
`//`, backslash, control chars и `/admin` fail-closed ведут в `/dashboard`. Draft переносит
только `amount`, Roblox username и gamepass id. Telegram return хранится в `sessionStorage`,
не подменяет одноразовый серверный auth challenge. Инвариант покрыт unit-тестом.

**WB handoff:** будущая выдача на сайте не должна использовать raw WB code как credential
для связывания identity. Пересланная/утёкшая deep link иначе позволяет захватить reservation
или создать заказ не тому пользователю. Требуется отдельный random challenge, SHA-256 hash
в БД, короткий TTL, consume-once, binding к reservation и idempotency `code+user`.

**Продуктовый риск:** требование подписаться как условие выдачи уже оплаченного на WB товара
создаёт спор, возвраты и обходы. Поэтому обязательным делается подтверждённый диалог/identity
TG или VK; membership проверяется сервером и может давать бонус, но не удерживает номинал.
План и rollout: `docs/site-launch-implementation-plan.md`, раздел 4.

### 24. IDOR в callback-кнопках ботов — ✅ ЗАКРЫТО 2026-07-26

`bots/shared/gp-watch-confirm.ts:26,71` принимает `orderId` строкой и **не проверяет, что
заказ принадлежит вызывающему**. Функции вызываются из `bots/tg/handlers.ts:3397,3420`
(`gpw_ok:` / `gpw_no:`) и `bots/vk/handlers.ts:818,834`. Та же дыра в
`handlers.ts:4426-4444` (`uci:` — отмена `DirectIntent` по ID).

**Почему это достижимо:** `callback_data` не является доверенным вводом. В Telegram любой
пользователь, у которого есть хоть одно сообщение бота с inline-клавиатурой, может через
неофициальный MTProto-клиент вызвать `messages.getBotCallbackAnswer` с **произвольной**
строкой `data`. В VK аналогично отправляется произвольный `payload`.

**Последствие:** чужой заказ переводится в `PENDING`, и при этом *предположительный* ник
(`probableNick`, найденный автоматикой) записывается в `robloxUsername` как подтверждённый
клиентом — комментарий в коде прямо называет его «authoritative». Дальше заказ уходит в
очередь на реальный выкуп робуксов на ник, который настоящий покупатель никогда не
подтверждал. Обратный вариант (`gpw_no:`) стирает `probableNick` у чужого заказа.

**Что ограничивает:** нужен точный `orderId` — CUID 25 символов, перебору не поддаётся.
Поэтому HIGH, а не критично. Но любая утечка ID (скриншот, пересланная карточка, ошибка в
логах) превращает это в потерю денег.

**Показательно:** соседние ветки того же файла сделаны правильно — `user_resubmit:`
(`handlers.ts:4988-4994`) и `ucd:` (`:4474`) владельца проверяют. Это пропуск, а не принятое
решение.

**План:** передавать в функции `actor` (платформа + внешний ID), резолвить `User` и
сверять `order.userId`; при несовпадении — отказ и лог `[gp-watch] ownership violation` как
сигнал атаки; вынести общий хелпер `assertOwnsOrder` и провести им аудит всех 55 веток
`callback_query`. Детали — `ultra-review-2026-07-25.md`, находка U6.

**✅ Закрыто 2026-07-26.** Появился общий хелпер `bots/shared/ownership.ts`
(`assertOwnsOrder` / `assertOwnsIntent`): сущность читается только вместе с резолвом
`User` по `tgId`/`vkId` и жёсткой сверкой владельца, нарушение пишется в лог как
`[ownership] violation …` — это сигнал атаки. `confirmGpWatch`/`declineGpWatch` теперь
требуют `actor` (платформа + внешний ID) и возвращают статус `forbidden`; то же для
`uci:`. Аудит всех 55 веток `callback_query` показал, что остальные ветки с ID сущности
либо закрыты `ADMIN_IDS.includes(...)`, либо резолвят сущность от самого пользователя
(`gp_pick:`, `dgp:`, `dp:` — по сессии), либо уже проверяли владельца (`ucd:`,
`user_resubmit:`). Тесты — `bots/shared/__tests__/ownership.test.ts` (7 шт., включая
«чужой заказ → forbidden» и «вызывающий вообще не наш пользователь»).

### 25. Бонусы и скидка списываются без компенсирующей записи — ✅ ЗАКРЫТО 2026-07-26

Не уязвимость извне, а системный дефект денежного учёта, поэтому фиксируется здесь наравне
с рисками: пользователь теряет накопленное, а расследовать это нечем.

`src/lib/canonical-web-order.ts:143-180` в транзакции создания заказа списывает `balance` и
обнуляет `rubleDiscount`. Компенсации нет ни на одном исходе: провал `Init`
(`api/orders/create/route.ts:153-171`), `REJECTED`/`CANCELED` от банка
(`api/webhooks/tinkoff/route.ts:105-107`), брошенная оплата. `BonusLedger` пишется только с
отрицательной дельтой; `balance: { increment }` во всём проекте встречается один раз —
бонус за отзыв.

В ботах симметрично: `bots/tg/handlers.ts:4476-4506` пытается вернуть бонус при отмене
прямого заказа, но берёт номинал из строки `WbCode`, которой у `DIR-` заказов **никогда не
существует** (`handlers.ts:2450`), поэтому ветка возврата недостижима. Скидка не
возвращается вовсе. Плюс `balance = user.balance + x` вместо `{ increment }` — lost update
при гонке, и запись в леджер не делается.

**Последствие:** `BonusLedger` не является источником правды, расхождение с `User.balance`
накапливается молча, спор с клиентом нечем закрыть.

**План:** единая точка `applyBonusDelta` (всегда `increment` + всегда идемпотентная запись в
леджер), компенсация `WEB_ORDER_REDEMPTION_REVERSED`, cron на протухшие
`AWAITING_PAYMENT`, хранение фактически применённой скидки в заказе, разовая сверка
`SUM(deltaRobux) == balance` и ручная компенсация пострадавшим с 12.07. Детали —
`ultra-review-2026-07-25.md`, находки U3/U4.

**✅ Закрыто 2026-07-26.**

- Единая точка изменения баланса: `src/lib/bonus-ledger.ts` (`applyBonusDeltaTx`) и её
  зеркало для ботов `bots/shared/order-benefits.ts`. Всегда `increment`, всегда запись в
  `BonusLedger`, всегда обязательный ключ идемпотентности, списание не уводит баланс в минус.
- В `WbOrder` появились `bonusAppliedRobux`, `discountAppliedKopecks` и
  `benefitsRevertedAt`: фактически применённые льготы теперь хранятся в заказе, а не
  «обнуляются в никуда».
- Компенсация `revertWebOrderBenefits` / `revertOrderBenefits` вызывается на всех исходах:
  провал `Init` в `orders/create`, `REJECTED`/`CANCELED` от банка в webhook, отмена
  клиентом (`ucd:`), и новый крон «брошенных» заказов (`AWAITING_PAYMENT`/`PAYMENT_PENDING`
  старше 2 ч → `REJECTED` + возврат), каждые 15 минут в TG-сервисе.
- `ucd:` больше не считает бонус из несуществующей строки `WbCode` — источник сумм сам
  заказ; клиенту и админу в сообщении об отмене показывается, что именно вернулось.
- Начисление бонуса за отзыв тоже переведено на леджер — раньше это был единственный
  `increment` в проекте и он не оставлял записи в журнале.
- Тесты: `bots/shared/__tests__/order-benefits.test.ts` (идемпотентность, отсутствие
  двойного возврата, «оплаченный заказ не компенсируется»).

**Историческое расхождение остаётся за владельцем.** Сверка 26.07
(`node scripts/bonus-ledger-audit.mjs`): **41 пользователь**, у каждого ±100 R$, суммарно
**4100 R$**. Ровно два паттерна: `balance=100 / ledger=0` (бонус за отзыв начислен без
записи) и `balance=0 / ledger=100` (бонус потрачен без записи). Скрипт умеет дописать
выравнивающие строки (`--apply`), но денег клиентам не начисляет — это решение владельца.

### 26. CSP всё ещё допускает `unsafe-inline`/`unsafe-eval` — 🟡 ЭТАП 1 (2026-07-26)

`next-security.ts`: боевая политика по-прежнему разрешает инлайн-скрипты и `eval` — на
платёжном сайте это главный отсутствующий барьер против XSS. Ужесточать вслепую нельзя:
гейт открывают из Telegram WebView и VK WebView, поломка входа там видна только по жалобам.

**Сделано (этап 1):** рядом с боевой политикой едет строгая `Content-Security-Policy-Report-Only`
(без `unsafe-inline`/`unsafe-eval`) с `report-uri /api/observability/csp-report`. Браузер
ничего не блокирует, но присылает всё, что строгая политика запретила бы; нарушения
логируются строкой `[csp-report] directive=… blocked=… doc=…`, роут за rate-limit.

**Дальше:** перевести инлайн-скрипт `src/app/twa/page.tsx` на nonce → убрать `unsafe-eval`
→ убрать `unsafe-inline` в `script-src` → enforce, и только после чистого отчёта на
iPhone / Android / Telegram WebView / VK WebView. План — `ultra-review-2026-07-25.md`, U10.

**⚠️ Инцидент 28.07: hash в боевой политике отменил `'unsafe-inline'`.** Вместе с D3
sha256-хеш boot-скрипта темы добавили не только в Report-Only, но и в боевую политику,
рядом с `'unsafe-inline'`. По спецификации CSP наличие hash- или nonce-источника
**отменяет** `'unsafe-inline'` в той же директиве — то есть политика фактически стала
строгой, и браузер заблокировал все inline-скрипты Next.js. Гидратация не запускалась:
`/login` и `/checkout` отдавали пустой экран, на главной ничего не реагировало на нажатия.
Замер на проде: `window.__next_f` пуст, в консоли `Executing inline script violates …`.
Server-rendered HTML при этом отдавался нормально, поэтому `smoke:site` и `smoke-corridor`
остались зелёными — они проверяют HTML, а не исполнение скриптов.

**Исправлено:** хеш убран из боевой политики и оставлен только в Report-Only. Инвариант
закреплён контракт-тестом `src/__tests__/theme-boot.test.ts`: пока в боевой политике есть
`'unsafe-inline'`, hash/nonce в ней запрещены. Вывод для будущего enforce: **добавление
хеша — это уже enforce**, промежуточного состояния «и хеш, и `unsafe-inline`» не бывает.

**Вторая причина, почему заметили поздно (исправлена 28.07).** У `<html>` в
`src/app/layout.tsx` не было `suppressHydrationWarning`, хотя boot-скрипт темы намеренно
правит этот узел до гидратации (`data-theme`, `data-theme-mode`, `color-scheme`). React
писал предупреждение о рассинхроне на **каждой** странице — проверено на `/`, `/faq`,
`/guarantees`, `/legal/details`, `/login`, `/checkout`. В этом постоянном шуме настоящая
поломка гидратации ничем не выделялась. Теперь подавление стоит ровно на `<html>`, остальное
дерево проверяется как обычно, и появление предупреждения снова означает реальную проблему.

### 27. Ретенция служебных данных — ✅ ЗАКРЫТО 2026-07-26

`PriceQuote`, `EmailActionToken` и `OrderEvent` не чистились никогда; в связке с обходом
rate-limit (риск №2) это был вектор неограниченного роста продовой БД анонимными запросами.

Добавлен суточный крон `bots/shared/retention.ts` в TG-сервисе: неиспользованные
`PriceQuote` с `expiresAt` старше 7 дней и без привязанного заказа — удаляются;
`EmailActionToken` с `expiresAt` старше 30 дней — удаляются; `OrderEvent` старше 18
месяцев только **считаются** и выводятся в лог (это аудит денежных операций, удаление —
решение владельца). `ConsentEvidence` не трогается принципиально: юридическое
доказательство согласия.

### 21. Коды активации Roblox — предъявительские секреты в БД — P0, 🔵 ПЛАН (2026-07-17)

Планируемый товар «коды активации» вводит в систему **предъявительский инструмент**: строка
кода сама по себе является деньгами, для её траты не нужен ни аккаунт, ни пароль. Это
принципиально опаснее всего, что есть сейчас: утёкший код невозможно «отозвать», а факт
кражи виден только по жалобе клиента.

**Риск:** утечка дампа БД или логов = прямая потеря денег; инсайдерская кража кода
менеджером; двойная выдача одного кода при гонке; оплата без выдачи и без возврата;
чарджбэк «код не работал» без доказательства раскрытия; мёртвые коды при нелегальном или
регионально несовместимом поставщике.

**Требования (обязательные, до первой продажи):** envelope-шифрование кода в покое (ключ в
env/KMS, не в публичном репозитории); запрет кода в любых логах/ошибках/уведомлениях с
contract-тестом; атомный резерв (`UPDATE … WHERE status='IN_STOCK'` / `SKIP LOCKED`) вместо
read-modify-write; идемпотентная выдача на `PaymentAttempt.idempotencyKey` + `OutboxMessage`;
аудит доступа менеджера; фиксация `revealedAt` как доказательство для спора; автоматический
возврат, если деньги взяты, а код не выдан. Логика родственна риску №15 (скретч-слой WB).

**План и открытые вопросы:** `docs/roblox-codes-plan.md`.

### 12. Детерминированный пароль в seed — ✅ ЗАКРЫТО В КОДЕ (2026-07-12)

`prisma/seed.js` содержит публичные admin email и фиксированный предсказуемый пароль.
Read-only проверка production на 2026-07-12 подтвердила: такой пользователь там отсутствует,
активного default-пароля нет. Немедленного компромета production не обнаружено, но будущий
запуск seed на новом окружении создаст известные всем credentials.

**Фикс `P0 foundation`:** seed больше не содержит
фиксированных credentials и создаёт admin только если одновременно переданы
`BOOTSTRAP_ADMIN_EMAIL` и `BOOTSTRAP_ADMIN_PASSWORD` длиной от 16 символов. Пароль хешируется
с cost 12, а оператор обязан сменить его после первого входа. Без обеих переменных seed не
создаёт privileged user. Примерные отзывы также не seed-ятся без явного локального
`SEED_DEMO_CONTENT=true` и помечаются непроверенными.

### 13. Публичная витрина: headers, abuse и глобальные SDK — HIGH, 🟡 ЧАСТИЧНО ЗАКРЫТО (2026-07-12)

На проверенных public routes нет полного набора HSTS/CSP/Referrer-Policy/
Permissions-Policy/frame-ограничений; виден `X-Powered-By`. Rate-limit закрывает только часть
WB API, но не checkout, регистрацию и дорогие Roblox lookup. Telegram WebApp SDK подключён
глобально и изменяет `<html>` до hydration даже на обычной витрине, что уже воспроизводит
hydration mismatch; VK SDK также загружается шире необходимого.

**Сделано `P0 foundation`:** `poweredByHeader: false`;
общие HSTS, nosniff, referrer/permissions policy и совместимый CSP в обоих Next-билдах;
отдельный `frame-ancestors` для `/twa`; Telegram SDK перенесён из root layout в TWA route,
VK ID SDK грузится только когда отображается `VKAuthButton`; добавлен in-memory rate limit на
регистрацию, checkout и публичные Roblox lookup, плюс unit-тест limiter.

**Остаётся до запуска:** проверка CSP и логинов на реальных TG/VK WebView; distributed
rate-limit перед горизонтальным масштабированием; убрать `unsafe-inline`/
`unsafe-eval` через nonce-based CSP после этой матрицы. Текущий limiter намеренно не
подменяет WAF/Redis при нескольких репликах.

### 3. `debug`-роут — ✅ ЗАКРЫТО (2026-07-03)
`/api/twa/debug` (за `ADMIN_SECRET` + `timingSafeEqual`) больше не отдаёт первые 12 символов
`WB_API_TOKEN`: в проде поле `tokenFirst12` = `"(hidden in prod)"`, длина токена остаётся для
диагностики. Полный превью доступен только при `NODE_ENV !== "production"`.

### 4. Захардкоженные IP серверов — ✅ ЗАКРЫТО (2026-07-03)
IP убраны из кода и комментариев (`Dockerfile`, `bots/tg/admin/hub-system.ts`,
`src/app/api/twa/system/route.ts`, `scripts/setup-swap.sh`). Health-URL ботов теперь
берутся **только** из env (`VK_BOT_HEALTH_URL`, `TG_BOT_HEALTH_URL`); без env сервис
отображается как «нет данных» (ok:false, ms:0). ⚠️ Перед деплоем убедиться, что обе
переменные заведены в Coolify (значения — в `HANDOFF.md`), иначе виджет здоровья
покажет ботов красными.

### 5. `vk-id` CredentialsProvider доверял клиенту — ✅ ЗАКРЫТО (2026-07-03)
**Было:** провайдер `vk-id` принимал `vk_id`/`name`/`image` из тела запроса **без серверной
проверки**; любой с CSRF-токеном логинился под произвольным VK ID.

**Фикс:** клиент (`VKAuthButton.tsx`) больше не шлёт личность — только сырые токены из
`VKID.Auth.exchangeCode()` (`access_token` + `id_token`). Сервер (`src/lib/vk-id.ts`,
`verifyVkIdUser`) резолвит личность через VK: `POST https://id.vk.com/oauth2/user_info`
(по `access_token`), фолбэк `.../oauth2/public_info` (по `id_token`), оба с `client_id`.
`vk_id`/имя/аватар берутся **только** из ответа VK. Проверено симуляцией атаки: подделанные
токены и старый формат (`vk_id` без токенов) → `CredentialsSignin`, сессия не создаётся.

**Побочный эффект:** ушёл клиентский fallback «VK User» — имя всегда из проверенного ответа
VK; вдобавок `authorize()` теперь обновляет `User.name`/`image` только непустыми значениями,
так что пустой ответ VK больше не затирает реальное имя в БД
(см. `docs/corridor-and-site.md`, «TG-уведомления при VK-логине»).

### 6. Историческая утечка в git — ✅ ПРОВЕРЕНО (2026-07-03), живых секретов нет
`HANDOFF.md`/`PLAN.md` были в истории git (выведены из трекинга, но история осталась).
Проведён аудит всех исторических ревизий этих файлов + всей истории:

- `NEXTAUTH_SECRET` (текущий) — **в истории не встречается**;
- пароль `DATABASE_URL` (текущий) — **в истории не встречается**; `.env*` никогда не коммитился;
- TG-бот-токены, VK-токены, `.ROBLOSECURITY`, JWT, Postgres-URL — **не найдены** (0 совпадений
  по паттернам);
- найдены только **2 старых Coolify-токена** (`18|…`, `27|…`) — оба давно просрочены/заменены
  (актуальный `30|…` живёт только в `HANDOFF.md`). Ротация не требуется; при желании можно
  отозвать их в Coolify → API Tokens для гигиены.

Вывод: переписывать историю (`git filter-repo`) не обязательно — в ней только инфраструктурные
описания (IP/UUID уже публичны) и мёртвые токены.

## select-gamepass — валидация
`api/wb-code/select-gamepass` ре-валидирует цену и `isActive`, но не проверяет принадлежность
геймпасса нику (`creatorId`). Hand-crafted POST может привязать чужой геймпасс с совпадающей
ценой. Митигируется ручным выкупом менеджером (он видит карточку). Низкий приоритет.

## Пароль production-терминала раскрыт — HIGH, ТРЕБУЕТ РОТАЦИИ (2026-07-27)

Password рабочего терминала был опубликован в операторской переписке и на скриншоте.
Значение не добавлено в git, документацию или логи, однако его нельзя считать секретным и
нельзя оставлять после диагностики. Исправленный регистр символа подтвердил рабочую пару
боевым payment/refund E2E, но не вернул секретность уже опубликованному значению.
Обязательное действие: перевыпустить Password в кабинете Т‑Банка, записать новый
секрет напрямую в production secret storage, перезапустить Web и никогда не передавать его
в чатах/скриншотах. Верификация должна выводить только факт совпадения/длину, не значение.

Для одного повторного E2E допускается временный `TBANK_DIAGNOSTIC_JSON_LOGS=true`. Он пишет
полный `Init` request/response для поддержки, но никогда не включает Password/SecretKey.
Запись всё равно чувствительна: содержит email, provider Token и bearer token callback URL.
Доступ — только инфраструктурным операторам; передача — только поддержке Т‑Банка; после
теста флаг выключается и Web перезапускается. Постоянное полное payment-логирование запрещено.

## Полный refund не закрывал очередь выкупа — HIGH, ИСПРАВЛЕНО (2026-07-27)

Первый production E2E показал рассинхрон: подписанный `REFUNDED` webhook корректно закрывал
`PaymentAttempt`, refund accounting и outbox, но оставлял незавершённый `WbOrder` в
`PENDING`. Такой заказ мог быть выкуплен после возврата денег. Тестовый заказ был снят с
выдачи до покупки gamepass. Webhook теперь переводит terminal `REFUNDED` в `REJECTED` для
любого order, который ещё не `COMPLETED`; частичный refund очередь не закрывает, а уже
исполненный заказ не переписывается задним числом. Матрица закреплена unit-тестом.

## Повтор `Init` и наблюдаемость outbox — HIGH, ИСПРАВЛЕНО ЛОКАЛЬНО (2026-07-28)

Старый checkout после 502 навсегда возвращал заказ без payment URL. Наивный повтор мог
дважды применить возвращённый бонус, а одинаковый provider `OrderId` смешал бы callback
разных попыток. Теперь один order имеет максимум три уникальные provider-попытки;
повторно резервируемые benefits защищены revision/idempotency, а поздний terminal callback
не закрывает order при наличии другой живой попытки.

Отдельный риск был в совместном отказе TG и payment-outbox: webhook и деньги сохранялись,
но администратор мог не получить уведомление. Durable heartbeat пишет TG-процесс. Первый
controlled production stop подтвердил stale detection Web, но одновременно доказал, что RF
Web transport возвращает `fetch failed`; `lastAlertAt` корректно сбросился и ложного успеха
не было. Основной alert consumer перенесён в отдельный VK-процесс, который остаётся жив при
остановке TG. Второй drill показал, что и его Telegram transport не подтверждает доставку.
Поэтому после отказа Telegram VK вызывает Web endpoint, а Web отправляет email через SMTP.
`POST /api/internal/worker-alert` принимает только четыре фиксированных event kind, текст до
2000 символов и constant-time сверяет `VALIDATOR_KEY`; получатели выводятся сервером из
проверенных email пользователей, чья TG identity входит в `ADMIN_IDS`. Клиент не может
указать адрес, endpoint не логирует адреса/ключ, а atomic DB dedupe ограничивает повтор разом
в час. Одновременный отказ TG+VK+Web/SMTP всё ещё требует внешнего инфраструктурного
мониторинга.

Rollout-скрипт по умолчанию fail-closed запрещает 10/50/on, пока timestamp production
`TINKOFF_SECRET_KEY` не новее зафиксированного раскрытия. Timestamp не доказывает смену
значения: оператор должен ротировать Password в кабинете банка, а не пересохранять старый.
Явное исключение допускается только после информированного решения владельца: оно выводит
`SECURITY EXCEPTION`, не меняет timestamp и фиксируется только в закрытом handoff.
